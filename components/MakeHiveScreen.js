import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const MakeHiveScreen = () => {
  return (
    // Replace with your actual settings screen UI
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text>Make Hive Screen</Text>
    </View>
  );
};

export default MakeHiveScreen;
